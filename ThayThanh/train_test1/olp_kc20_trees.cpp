#include <bits/stdc++.h>

#define ll long long

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, m, k;
    cin>>n>>m>>k;
    vector<int> a(m);
    for(int &x : a) cin>>x;
    sort(a.begin(),a.end());
    int l = 0, h=n-1;
    for(int i = 0; i < n; i++){

    }
    return 0;
}